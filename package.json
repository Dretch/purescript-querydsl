{
  "name": "purescript-querydsl",
  "license": "WTFPL",
  "dependencies": {
    "sqlite3": "^4.0.8"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^13.0.0",
    "purescript": "^0.13.0"
  },
  "scripts": {
    "clean": "rm -rf .pulp-cache bower_components node_modules output",
    "install": "bower install",
    "build": "pulp build",
    "build:ide": "pulp build -- --json-errors",
    "test": "pulp test"
  }
}
