{
  "name": "purescript-querydsl",
  "license": "WTFPL",
  "dependencies": {
    "sqlite3": "^4.0.8"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^13.0.0",
    "purescript": "^0.13.4",
    "spago": "^0.9.0"
  },
  "scripts": {
    "clean": "rm -rf .pulp-cache .spago bower_components node_modules output",
    "build": "spago build",
    "build:ide": "spago build -- --json-errors",
    "test": "spago test"
  }
}
